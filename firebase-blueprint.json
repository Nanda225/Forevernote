{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User profile with partner information for GenZ couples",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's display name"
        },
        "email": {
          "type": "string",
          "description": "The user's email address"
        },
        "partnerName": {
          "type": "string",
          "description": "The custom display name of the partner"
        },
        "partnerEmail": {
          "type": "string",
          "description": "The partner's email address"
        },
        "anniversaryDate": {
          "type": "string",
          "format": "date",
          "description": "The starting date of the relationship"
        },
        "inviteCode": {
          "type": "string",
          "description": "Unique invite code of the user for partner pairing"
        },
        "partnerInviteCode": {
          "type": "string",
          "description": "The invite code entered to pair with their partner"
        },
        "connectedPartnerId": {
          "type": "string",
          "description": "The UID of the linked partner user in Firebase"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the profile was created"
        }
      },
      "required": ["name", "email", "createdAt"]
    },
    "LoveMessage": {
      "title": "Private Love Message",
      "description": "A secret encrypted-like emotion note shared securely inside the couple space",
      "type": "object",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "UID of the sender"
        },
        "recipientId": {
          "type": "string",
          "description": "UID of the partner recipient"
        },
        "text": {
          "type": "string",
          "description": "The content of the private message"
        },
        "style": {
          "type": "string",
          "description": "Visual theme of the card or message style"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the secret note was created"
        }
      },
      "required": ["senderId", "recipientId", "text", "style", "createdAt"]
    },
    "Milestone": {
      "title": "Milestone Event",
      "description": "A special relationship or friendship milestone event",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The creator's UID"
        },
        "title": {
          "type": "string",
          "description": "Name of the milestone (e.g. First Meet)"
        },
        "type": {
          "type": "string",
          "enum": ["First Meet", "First Date", "First Talk", "First Trip", "Proposal", "Anniversary", "Custom"],
          "description": "Category of the milestone"
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "The exact date of the milestone (YYYY-MM-DD)"
        },
        "description": {
          "type": "string",
          "description": "A sweet description of what happened"
        },
        "aiLetterStyle": {
          "type": "string",
          "enum": ["Romantic", "Playful", "Cute", "Nostalgic", "Sarcastic", "GenZ Slang"],
          "description": "The tone of the Gemini-generated letter"
        },
        "generatedLetter": {
          "type": "string",
          "description": "The letter content generated by Gemini AI"
        },
        "imageUrl": {
          "type": "string",
          "description": "Optional image attachment for the milestone"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the milestone was recorded"
        }
      },
      "required": ["userId", "title", "type", "date", "description", "createdAt"]
    },
    "Reminder": {
      "title": "Milestone Reminder",
      "description": "Scheduled reminder for the milestone",
      "type": "object",
      "properties": {
        "milestoneId": {
          "type": "string",
          "description": "The ID of the milestone"
        },
        "userId": {
          "type": "string",
          "description": "The owner's UID"
        },
        "deliveryType": {
          "type": "string",
          "enum": ["Email", "In-App"],
          "description": "How the reminder is delivered"
        },
        "scheduledDate": {
          "type": "string",
          "format": "date",
          "description": "The date when the reminder should trigger"
        },
        "status": {
          "type": "string",
          "enum": ["pending", "sent"],
          "description": "Status of the delivery"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the reminder was created"
        }
      },
      "required": ["milestoneId", "userId", "deliveryType", "scheduledDate", "status", "createdAt"]
    },
    "GalleryItem": {
      "title": "Gallery Image Card Keepsake",
      "description": "A sweet visual photo scrapbook card shared between partners",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Creator's real UID"
        },
        "partnerId": {
          "type": "string",
          "description": "Partner's real UID"
        },
        "title": {
          "type": "string",
          "description": "Exquisite romantic title of the keepsake card"
        },
        "caption": {
          "type": "string",
          "description": "The sweet visual handwritten diary details"
        },
        "imageUrl": {
          "type": "string",
          "description": "Double photo path link URL"
        },
        "category": {
          "type": "string",
          "description": "The designated collage category (e.g. Travel, Dates, Selfies)"
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "The custom timestamp date of the visual keepsake (YYYY-MM-DD)"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When this ledger card was established"
        }
      },
      "required": ["userId", "title", "caption", "imageUrl", "category", "date"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "Stores user profiles"
    },
    "love_messages/{messageId}": {
      "schema": "LoveMessage",
      "description": "Stores private secret feelings or messages shared between partners"
    },
    "milestones/{milestoneId}": {
      "schema": "Milestone",
      "description": "Stores milestone events of the couple/friends"
    },
    "reminders/{reminderId}": {
      "schema": "Reminder",
      "description": "Stores push/email notification reminders"
    },
    "gallery_items/{photoId}": {
      "schema": "GalleryItem",
      "description": "Stores shared collage keepsakes"
    }
  }
}
